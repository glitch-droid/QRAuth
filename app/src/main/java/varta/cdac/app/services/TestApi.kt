package varta.cdac.app.services

import retrofit2.Call
import retrofit2.http.Body
import retrofit2.http.POST
import varta.cdac.app.model.QRtoken

interface TestApi {

    //This POST is for testing purposes only
    /*@POST("posts")
        fun sendData(
            @Body data : Data
        ) : Call<Data>*/

    /*@POST("/users/login")
        fun sendQRToken(
            @Body qrData:QRtoken
        ) : Call<QRtoken>*/
}